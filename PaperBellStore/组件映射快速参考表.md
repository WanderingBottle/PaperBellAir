# MudBlazor → Blazorise 组件映射快速参考表

## 🚀 快速查找

### 布局组件

| MudBlazor                  | Blazorise                                                     | 关键属性映射                   |
| -------------------------- | ------------------------------------------------------------- | ------------------------------ |
| `<MudContainer>`           | `<Container>`                                                 | `MaxWidth` → 移除（使用 CSS）  |
| `<MudCard>`                | `<Card>`                                                      | `Elevation` → 移除（使用 CSS） |
| `<MudCardContent>`         | `<CardBody>`                                                  | -                              |
| `<MudGrid>`                | `<Row>`                                                       | `Spacing` → `Gutter`           |
| `<MudItem xs="12" sm="6">` | `<Column ColumnSize="ColumnSize.Is12.OnMobile.Is6.OnTablet">` | -                              |
| `<MudStack Row="true">`    | `<Row>`                                                       | `Spacing` → `Gutter`           |
| `<MudStack>`               | `<Column>`                                                    | `Spacing` → `Gutter`           |
| `<MudPaper>`               | `<Card>`                                                      | `Elevation` → 移除             |
| `<MudDivider>`             | `<Divider>`                                                   | -                              |

### 文本和图标

| MudBlazor                                        | Blazorise                       | 关键属性映射                 |
| ------------------------------------------------ | ------------------------------- | ---------------------------- |
| `<MudText Typo="Typo.h5">`                       | `<h5>` 或 `<Text>`              | `Typo` → HTML 标签           |
| `<MudText>`                                      | `<p>` 或 `<Text>`               | -                            |
| `<MudIcon Icon="@Icons.Material.Filled.Search">` | `<Icon Name="IconName.Search">` | Material Icons → FontAwesome |

### 表单组件

| MudBlazor         | Blazorise      | 关键属性映射                                          |
| ----------------- | -------------- | ----------------------------------------------------- |
| `<MudButton>`     | `<Button>`     | `Variant` → `Color`, `StartIcon` → `<Icon>` 子元素    |
| `<MudTextField>`  | `<TextEdit>`   | `Label` → `<FieldLabel>`, `Variant` → 移除            |
| `<MudSelect>`     | `<Select>`     | `Label` → `<FieldLabel>`, `Clearable` → 移除          |
| `<MudSelectItem>` | `<SelectItem>` | -                                                     |
| `<MudDatePicker>` | `<DateEdit>`   | `Label` → `<FieldLabel>`, `TimePicker` → 需要额外配置 |
| `<MudCheckBox>`   | `<Check>`      | `Label` → 文本内容                                    |

### 数据展示

| MudBlazor             | Blazorise         | 关键属性映射                                       |
| --------------------- | ----------------- | -------------------------------------------------- |
| `<MudDataGrid>`       | `<DataGrid>`      | `ServerData` → `ReadData`, `FilterMode` → 移除     |
| `<MudDataGridPager>`  | `<DataGrid>` 内置 | `ShowPager="true"`                                 |
| `<MudChip>`           | `<Badge>`         | `Size` → 移除, `Color` → `Color`                   |
| `<MudTooltip>`        | `<Tooltip>`       | `Text` → `Text`                                    |
| `<MudProgressLinear>` | `<Progress>`      | `Indeterminate` → `Animated`                       |
| `<MudAlert>`          | `<Alert>`         | `Severity` → `Color`                               |
| `<MudPagination>`     | `<Pagination>`    | `Count` → `TotalItems`, `Selected` → `CurrentPage` |

### 交互组件

| MudBlazor              | Blazorise    | 关键属性映射                |
| ---------------------- | ------------ | --------------------------- |
| `<MudDialog>`          | `<Modal>`    | 完全不同的 API              |
| `<MudTabs>`            | `<Tabs>`     | `Elevation` → 移除          |
| `<MudTabPanel>`        | `<Tab>`      | `Text` → `Title`            |
| `<MudExpansionPanels>` | `<Collapse>` | `MultiExpansion` → 移除     |
| `<MudExpansionPanel>`  | `<Collapse>` | `Text` → `<CollapseHeader>` |

### 服务接口

| MudBlazor            | Blazorise         | 方法映射                                             |
| -------------------- | ----------------- | ---------------------------------------------------- |
| `IDialogService`     | `IModalService`   | `ShowAsync<T>()` → `Show<T>()`                       |
| `ISnackbar`          | `IMessageService` | `Add()` → `Success()`/`Info()`/`Warning()`/`Error()` |
| `IMudDialogInstance` | `ModalInstance`   | `Cancel()` → `Hide()`                                |

## 📝 常用代码片段

### 按钮

```razor
<!-- MudBlazor -->
<MudButton Color="@MudBlazor.Color.Primary" StartIcon="@Icons.Material.Filled.Search">
    搜索
</MudButton>

<!-- Blazorise -->
<Button Color="Color.Primary">
    <Icon Name="IconName.Search" />
    搜索
</Button>
```

### 文本输入框

```razor
<!-- MudBlazor -->
<MudTextField @bind-Value="text" Label="名称" Variant="Variant.Outlined" />

<!-- Blazorise -->
<Field>
    <FieldLabel>名称</FieldLabel>
    <TextEdit @bind-Text="text" />
</Field>
```

### 下拉选择

```razor
<!-- MudBlazor -->
<MudSelect T="string" @bind-Value="value" Label="选择">
    <MudSelectItem Value="1">选项1</MudSelectItem>
</MudSelect>

<!-- Blazorise -->
<Field>
    <FieldLabel>选择</FieldLabel>
    <Select TValue="string" SelectedValue="value"
            SelectedValueChanged="@((string v) => value = v)">
        <SelectItem Value="1">选项1</SelectItem>
    </Select>
</Field>
```

### 对话框

```razor
<!-- MudBlazor -->
@inject IDialogService DialogService
await DialogService.ShowAsync<MyDialog>("标题", parameters, options);

<!-- Blazorise -->
@inject IModalService ModalService
var modal = ModalService.Show<MyDialog>("标题");
modal.ModalInstance.SetParameters(ParameterView.FromDictionary(parameters));
```

### 消息提示

```razor
<!-- MudBlazor -->
@inject ISnackbar Snackbar
Snackbar.Add("成功", Severity.Success);

<!-- Blazorise -->
@inject IMessageService MessageService
await MessageService.Success("成功");
```

## 🎨 颜色映射

| MudBlazor         | Blazorise         |
| ----------------- | ----------------- |
| `Color.Primary`   | `Color.Primary`   |
| `Color.Secondary` | `Color.Secondary` |
| `Color.Success`   | `Color.Success`   |
| `Color.Info`      | `Color.Info`      |
| `Color.Warning`   | `Color.Warning`   |
| `Color.Error`     | `Color.Danger`    |
| `Color.Dark`      | `Color.Dark`      |

## 📦 图标映射（Material Icons → FontAwesome）

| Material Icon                      | FontAwesome                    |
| ---------------------------------- | ------------------------------ |
| `Icons.Material.Filled.Search`     | `IconName.Search`              |
| `Icons.Material.Filled.Refresh`    | `IconName.Refresh`             |
| `Icons.Material.Filled.Filter`     | `IconName.Filter`              |
| `Icons.Material.Filled.List`       | `IconName.List`                |
| `Icons.Material.Filled.Visibility` | `IconName.Eye`                 |
| `Icons.Material.Filled.Info`       | `IconName.Info`                |
| `Icons.Material.Filled.Error`      | `IconName.ExclamationTriangle` |
| `Icons.Material.Filled.Close`      | `IconName.Times`               |
| `Icons.Material.Filled.Person`     | `IconName.User`                |
| `Icons.Material.Filled.Computer`   | `IconName.Desktop`             |
| `Icons.Material.Filled.Code`       | `IconName.Code`                |
| `Icons.Material.Filled.PlayArrow`  | `IconName.Play`                |

## ⚠️ 特殊注意事项

1. **日期时间选择器**：Blazorise 的 `DateEdit` 默认不支持时间选择，需要额外配置或使用第三方组件
2. **对话框参数**：Blazorise Modal 的参数传递方式不同，需要使用 `SetParameters`
3. **数据表格**：`ServerData` 委托签名不同，需要调整参数类型
4. **分页器**：Blazorise DataGrid 内置分页，不需要单独的 Pager 组件
